# ShopApp

This is the main repository for the ShopApp project, containing both the backend and frontend components.

## Project Structure

- **`backend/`**: The backend server application (built with Java Spring Boot).
- **`frontend/`**: The mobile frontend application (built with Flutter).
- **`.github/`**: GitHub actions and workflows.
